ESP32-BLE-Gamepad (核心库)

作用：让你的ESP32能被电脑识别为一个蓝牙游戏手柄，是处理按键、摇杆等所有输入功能的核心。

安装：在Arduino IDE中，打开 工具 > 管理库，搜索 ESP32-BLE-Gamepad 并安装。

NimBLE-Arduino (依赖库)

作用：它是一个更现代、更轻量的蓝牙栈，新版本的 ESP32-BLE-Gamepad 库（尤其是3.0以上版本）需要它才能正常工作。

安装：在Arduino IDE的库管理器中，搜索 NimBLE-Arduino 并安装。
